_base_: default.yaml

algorithm: ActorCritic

# 训练控制（step-based）
total_steps: 100000
log_interval: 1024
eval_interval: 5120
save_interval: 20480

# 超参
lr: 3e-4
gamma: 0.99
entropy_coef: 0.01
rollout_length: 128
n_step: 5

# 网络
hidden_dims: [128, 128]

evaluation:
  total_episodes: 10
