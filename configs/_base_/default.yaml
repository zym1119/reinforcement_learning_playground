# 全局默认配置，所有算法的 base config 都应继承此文件

env: CartPole-v1
env_kwargs: {}
seed: 42
device: auto
work_dir: ./work_dirs
run_name: null

# 训练控制
total_steps: 100000
log_interval: 1000
eval_interval: 10000
save_interval: 20000

# 通用超参
lr: 3e-4
gamma: 0.99

# 网络结构
hidden_dims: [64, 64]
activation: relu
